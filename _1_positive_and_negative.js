/*

Given an integer input, the objective is check whether the given integer is Positive or Negative. 

*/

const numPosNeg = (num) => {
    console.log( num >= 0 ? num +  " is positive number" : num + " is Negative number");
}

numPosNeg(14);
