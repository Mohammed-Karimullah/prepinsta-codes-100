/*

Given two integer input numbers, the objective is to 
find the power of the number raise to the power variable

*/

const powOfNum = (n,p) => {
    console.log(Math.pow(n,p));
}

powOfNum(3,2);